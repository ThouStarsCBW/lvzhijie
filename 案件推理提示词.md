# 修改任务提示词：让案件推理追问发送在 Mock 微信模式下只写入本地模拟聊天

## 0. 任务背景

你正在修改一个法律工作台项目。

项目根目录是：

```text
C:\Users\35696\Desktop\law-project
```

本次只允许修改 `lvzhijie` 这个子项目。

不要修改：

```text
mission-control/
```

本项目现在的微信功能已经变成了一个简单的 mock 演示系统。也就是说，在 mock 模式下，任何“发送到微信”的动作都不应该真的触碰 OpenClaw Gateway，也不应该真的发送到微信用户那里。

本次任务要修正的是：

```text
案件推理模块里的“追问问题发送”
```

目标是让它在 mock 模式下只写入本地 mock 微信聊天记录。

---

## 1. 绝对禁止事项

请严格遵守以下限制。

禁止做这些事：

1. 不要修改 `mission-control` 目录。
2. 不要重构整个项目。
3. 不要删除已有字段。
4. 不要修改 `.env` 文件。
5. 不要读取、打印、提交、暴露任何 API Key、token、gateway_token。
6. 不要改大模型接入逻辑。
7. 不要改 OpenClaw Gateway RPC 协议。
8. 不要把 mock 发送改成真实微信发送。
9. 不要新增数据库或 JSON 存储结构。
10. 不要改 `WechatMessage.status` 枚举。
11. 不要新增 `sent_via_mock`、`sent_in_mock` 等新状态。
12. 不要改 UI 布局。
13. 不要改业务无关页面。
14. 不要顺手修其它无关 bug。

本次只做三个方面：

1. 后端追问发送接口增加显式 mock 分支。
2. 增加测试，确保 mock 模式不会调用真实 OpenClaw。
3. 前端文案改成中性说法，避免误导用户以为真的发到了微信。

---

## 2. 本次允许修改的文件

优先只修改以下文件：

```text
lvzhijie/backend/app/main.py
lvzhijie/backend/tests/test_mock_wechat.py
lvzhijie/backend/tests/test_mvp_flows.py
lvzhijie/frontend/src/views/CaseDetailView.vue
lvzhijie/frontend/src/views/ReasoningView.vue
```

如果没有必要，不要修改其它文件。

---

## 3. 当前数据存储方式

后端使用 JSON 文件存储数据。

主数据文件：

```text
lvzhijie/backend/app/data/store.json
```

Mock 微信源数据目录：

```text
lvzhijie/backend/app/data/mock_wechat/
```

Mock 微信会话文件：

```text
lvzhijie/backend/app/data/mock_wechat/conversations.json
```

Mock 微信消息文件：

```text
lvzhijie/backend/app/data/mock_wechat/messages/{conversation_id}.json
```

Mock 微信附件目录：

```text
lvzhijie/backend/app/data/mock_wechat/assets/
```

`store.json` 中有这些关键表：

```json
{
  "openclaw_connection": {},
  "wechat_contacts": [],
  "wechat_conversations": [],
  "wechat_messages": [],
  "cases": [],
  "case_memories": [],
  "legal_reasoning_runs": [],
  "follow_up_questions": [],
  "activity_events": []
}
```

是否处于 mock 模式由这个字段决定：

```json
{
  "openclaw_connection": {
    "transport_mode": "mock"
  }
}
```

如果 `transport_mode` 是：

```text
mock
```

则任何发送动作都只能写入本地 mock 微信数据。

如果 `transport_mode` 是：

```text
gateway_rpc
```

才可以走真实 OpenClaw Gateway 发送。

---

## 4. 现有后端接口说明

### 4.1 普通微信发送接口

文件：

```text
lvzhijie/backend/app/main.py
```

已有接口：

```python
@app.post("/api/wechat/conversations/{conversation_id}/send")
async def send_wechat_message(conversation_id: str, payload: SendMessageRequest) -> WechatMessage:
```

这个接口现在已经正确处理 mock 模式。

它的逻辑是：

```python
if using_mock_wechat():
    new_msg = mock_wechat.append_message(
        conversation_id=conversation_id,
        sender="owner",
        content=payload.content,
    )
    message = WechatMessage.model_validate(new_msg)
    mock_wechat.sync_to_json_store(store)
    return message
```

这个行为是正确的。

不要破坏它。

### 4.2 案件追问发送接口

文件：

```text
lvzhijie/backend/app/main.py
```

当前接口：

```python
@app.post("/api/cases/{case_id}/follow-up-questions/{question_id}/send")
async def send_follow_up_question(
    case_id: str,
    question_id: str,
    payload: SendFollowUpRequest | None = None,
) -> dict[str, object]:
```

这个接口现在的问题是：它没有像普通微信发送接口一样显式写入 mock 微信文件。

当前代码里会直接调用：

```python
message = await OpenClawWechatAdapter(connection()).send_wechat_message(
    conversation_id=conversation.openclaw_conversation_id,
    content=content,
)
```

虽然 `OpenClawWechatAdapter` 内部也有 mock 判断，但是在 mock 模式下它只返回一个 mock message 对象，不会写入：

```text
mock_wechat/messages/{conversation_id}.json
```

所以必须改。

---

## 5. 后端必须实现的行为

修改：

```text
lvzhijie/backend/app/main.py
```

找到函数：

```python
send_follow_up_question
```

在它完成以下校验之后再改发送逻辑：

1. 案件存在。
2. 案件绑定了微信会话。
3. 会话存在。
4. 追问问题存在。
5. 最终发送内容 `content` 非空。

也就是在这段之后：

```python
content = (payload.content if payload and payload.content else question.content).strip()
if not content:
    raise HTTPException(status_code=422, detail="Follow-up content is empty")
```

把原本直接调用 `OpenClawWechatAdapter` 的逻辑改成显式分支。

必须实现如下逻辑：

```python
if using_mock_wechat():
    new_msg = mock_wechat.append_message(
        conversation_id=conversation.id,
        sender="owner",
        content=content,
    )
    message = WechatMessage.model_validate(new_msg)
    mock_wechat.sync_to_json_store(store)
    event_type = "follow_up.sent_mock"
    event_title = "通过演示模式发送追问"
else:
    message = await OpenClawWechatAdapter(connection()).send_wechat_message(
        conversation_id=conversation.openclaw_conversation_id,
        content=content,
    )
    message.conversation_id = conversation.id
    store.add("wechat_messages", message)
    event_type = "follow_up.sent"
    event_title = "通过 OpenClaw 发送追问"
```

### 5.1 mock 分支必须满足

mock 分支必须满足：

1. 必须调用 `using_mock_wechat()` 判断。
2. 必须调用 `mock_wechat.append_message(...)`。
3. `conversation_id` 必须传 `conversation.id`。
4. `sender` 必须是 `"owner"`。
5. `content` 必须是追问内容。
6. 必须调用 `WechatMessage.model_validate(new_msg)`。
7. 必须调用 `mock_wechat.sync_to_json_store(store)`。
8. 不允许调用 `OpenClawWechatAdapter`。
9. 不允许调用真实 OpenClaw Gateway。

### 5.2 gateway_rpc 分支必须满足

gateway_rpc 分支必须保持原行为：

1. 调用 `OpenClawWechatAdapter(connection()).send_wechat_message(...)`。
2. `conversation_id` 传 `conversation.openclaw_conversation_id`。
3. 发送后把 `message.conversation_id` 改回内部会话 id：

```python
message.conversation_id = conversation.id
```

4. 调用：

```python
store.add("wechat_messages", message)
```

### 5.3 后续更新逻辑

发送分支结束后，继续执行原来的更新逻辑。

必须保留并适配：

```python
conversation.last_message_at = message.created_at
store.update("wechat_conversations", conversation)

question.content = content
question.status = "sent_via_openclaw" if message.status == "sent_via_openclaw" else "failed"
question.sent_message_id = message.id
question.failure_reason = (
    str(message.raw_payload.get("error")) if message.raw_payload and message.raw_payload.get("error") else None
)
question.updated_at = now_iso()
store.update("follow_up_questions", question)
```

注意：不要改 `question.status` 的取值逻辑。

即使 mock 模式下，`message.status` 仍然可能是：

```text
sent_via_openclaw
```

这是现有兼容逻辑，本次不要改。

### 5.4 activity 记录

原来的 `record(...)` 要改成使用分支里的变量：

```python
record(
    event_type,
    event_title,
    content[:120],
    entity_type="case",
    entity_id=case_id,
)
```

不要继续在 mock 模式下记录：

```text
通过 OpenClaw 发送追问
```

mock 模式下应该记录：

```text
通过演示模式发送追问
```

---

## 6. 不要新增消息状态

现有模型里 `WechatMessage.status` 包含：

```text
sent_via_openclaw
```

虽然 mock 模式下这个名称不够准确，但是本次不要改。

不要新增：

```text
sent_via_mock
sent_in_mock
mock_sent
```

原因：

1. 会扩大模型修改范围。
2. 会影响前端类型。
3. 会影响已有测试。
4. 会影响旧数据兼容。

本次只修发送路径。

---

## 7. 前端文案修改要求

目标：不要让用户误以为 mock 模式真的发到了微信。

只改文案，不改布局，不改交互逻辑。

### 7.1 修改 CaseDetailView.vue

文件：

```text
lvzhijie/frontend/src/views/CaseDetailView.vue
```

找到推理/追问区域。

把以下文案：

```text
发送动作通过微信桥通道完成。
发微信
追问已通过微信桥发送。
请检查微信桥配置
```

改为：

```text
发送动作通过当前聊天通道完成。
发送
追问已写入当前聊天通道。
请检查聊天通道配置
```

不要改按钮的 `@click`。

不要改 `api.sendFollowUpQuestion(...)` 调用。

只改显示给用户看的文字。

### 7.2 修改 ReasoningView.vue

文件：

```text
lvzhijie/frontend/src/views/ReasoningView.vue
```

把以下文案：

```text
可以一键经微信桥发送给微信用户。
发微信
追问已通过微信桥发送。
请检查微信桥配置
```

改为：

```text
可以一键发送到当前聊天通道。
发送
追问已写入当前聊天通道。
请检查聊天通道配置
```

同样不要改按钮逻辑。

---

## 8. 后端测试要求

必须增加测试，确保 mock 模式下追问发送写入 mock 消息文件。

优先修改文件：

```text
lvzhijie/backend/tests/test_mock_wechat.py
```

### 8.1 测试一：追问发送写入 mock messages

新增测试函数：

```python
def test_follow_up_send_in_mock_mode_writes_to_mock_messages(client: TestClient) -> None:
```

测试步骤如下。

#### 第一步：设置 mock 模式

```python
connection = OpenClawConnection(transport_mode="mock")
assert client.put("/api/openclaw/connection", json=connection.model_dump()).status_code == 200
```

#### 第二步：生成推理

使用 demo case：

```python
generated = client.post("/api/reasoning/cases/case_demo/generate")
assert generated.status_code == 200
```

#### 第三步：获取追问问题

```python
detail = client.get("/api/cases/case_demo").json()
questions = detail["follow_up_questions"]
assert questions
question = questions[0]
```

#### 第四步：发送追问

```python
sent = client.post(
    f"/api/cases/case_demo/follow-up-questions/{question['id']}/send",
    json={},
)
assert sent.status_code == 200
```

#### 第五步：检查 mock 消息文件接口

```python
messages = client.get("/api/mock-wechat/conversations/conv_demo/messages").json()
```

断言追问被写入 mock 消息：

```python
assert any(
    m["content"] == question["content"]
    and m["sender"] == "owner"
    and m["direction"] == "outbound"
    for m in messages
)
```

#### 第六步：检查返回值兼容

```python
payload = sent.json()
assert payload["question"]["status"] == "sent_via_openclaw"
assert payload["message"]["sender"] == "owner"
assert payload["message"]["direction"] == "outbound"
```

### 8.2 测试二：mock 模式不调用 OpenClaw adapter

新增测试函数：

```python
def test_follow_up_send_in_mock_mode_does_not_call_openclaw_adapter(
    client: TestClient,
    monkeypatch: pytest.MonkeyPatch,
) -> None:
```

测试逻辑：

1. 先设置 mock 模式。
2. monkeypatch `OpenClawWechatAdapter.send_wechat_message`。
3. 如果这个方法被调用，就抛出 AssertionError。
4. 然后发送追问。
5. 接口必须成功。

示例：

```python
connection = OpenClawConnection(transport_mode="mock")
assert client.put("/api/openclaw/connection", json=connection.model_dump()).status_code == 200

async def fail_if_called(*args, **kwargs):
    raise AssertionError("OpenClaw adapter must not be called in mock mode")

monkeypatch.setattr(
    main.OpenClawWechatAdapter,
    "send_wechat_message",
    fail_if_called,
)

generated = client.post("/api/reasoning/cases/case_demo/generate")
assert generated.status_code == 200

detail = client.get("/api/cases/case_demo").json()
question = detail["follow_up_questions"][0]

sent = client.post(
    f"/api/cases/case_demo/follow-up-questions/{question['id']}/send",
    json={},
)
assert sent.status_code == 200
```

注意：

`test_mock_wechat.py` 文件开头已经可能有：

```python
import app.main as main
```

如果没有，则添加。

如果没有导入 `pytest`，则添加：

```python
import pytest
```

不要重复导入。

---

## 9. 可能需要调整的旧测试

文件：

```text
lvzhijie/backend/tests/test_mvp_flows.py
```

里面可能已有测试：

```python
def test_reasoning_creates_followups_and_mock_send(...)
```

这个测试原本只检查发送返回值。可以保留。

如果你发现它因为新增 mock 文件写入逻辑而失败，不要删除测试。应当修正预期，使其符合新行为。

---

## 10. API 使用说明

### 10.1 设置 OpenClaw 连接为 mock

请求：

```http
PUT /api/openclaw/connection
```

JSON：

```json
{
  "id": "local_openclaw",
  "name": "本机 OpenClaw",
  "gateway_url": "ws://localhost:18789",
  "gateway_token": null,
  "workspace_root": "~/.openclaw",
  "transport_mode": "mock",
  "gateway_protocol_version": 0,
  "allow_insecure_tls": false,
  "disable_device_pairing": false,
  "wechat_session_filter": "",
  "list_method": "sessions.list",
  "history_method": "chat.history",
  "send_method": "chat.send",
  "history_limit": 80,
  "enabled": true,
  "last_checked_at": null,
  "last_sync_at": null
}
```

测试中不要手写完整 JSON，直接用：

```python
connection = OpenClawConnection(transport_mode="mock")
client.put("/api/openclaw/connection", json=connection.model_dump())
```

### 10.2 生成案件推理

请求：

```http
POST /api/reasoning/cases/{case_id}/generate
```

示例：

```python
client.post("/api/reasoning/cases/case_demo/generate")
```

返回 `LegalReasoningRun`，同时会在 `follow_up_questions` 表里创建追问问题。

### 10.3 获取案件详情

请求：

```http
GET /api/cases/{case_id}
```

示例：

```python
detail = client.get("/api/cases/case_demo").json()
```

返回里包含：

```json
{
  "case": {},
  "tasks": [],
  "memories": [],
  "documents": [],
  "reasoning_runs": [],
  "follow_up_questions": [],
  "reply_jobs": [],
  "messages": []
}
```

### 10.4 发送追问

请求：

```http
POST /api/cases/{case_id}/follow-up-questions/{question_id}/send
```

请求体可以为空对象：

```json
{}
```

也可以覆盖追问内容：

```json
{
  "content": "请补充劳动合同签订时间。"
}
```

mock 模式下，这个接口必须写入：

```text
mock_wechat/messages/{conversation_id}.json
```

### 10.5 查询 mock 消息

请求：

```http
GET /api/mock-wechat/conversations/{conversation_id}/messages
```

示例：

```python
messages = client.get("/api/mock-wechat/conversations/conv_demo/messages").json()
```

返回消息数组。

其中 owner 发送的消息应该类似：

```json
{
  "id": "msg_xxx",
  "conversation_id": "conv_demo",
  "sender": "owner",
  "direction": "outbound",
  "type": "text",
  "content": "追问内容",
  "attachments": [],
  "status": "sent_via_openclaw",
  "openclaw_message_id": null,
  "created_at": "..."
}
```

---

## 11. 验证命令

在 Windows PowerShell 里执行。

进入后端目录：

```powershell
cd C:\Users\35696\Desktop\law-project\lvzhijie\backend
```

优先运行：

```powershell
.\.venv\Scripts\pytest.exe tests\test_mock_wechat.py tests\test_mvp_flows.py
```

如果 `pytest.exe` 不存在，运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_mock_wechat.py tests\test_mvp_flows.py
```

前端如果需要检查类型或构建，可以进入：

```powershell
cd C:\Users\35696\Desktop\law-project\lvzhijie\frontend
```

然后运行：

```powershell
npm run build
```

如果本项目没有 build 脚本，运行：

```powershell
npm run typecheck
```

如果也没有 typecheck 脚本，不要擅自创建脚本。

---

## 12. 最终验收标准

完成后必须满足全部条件：

1. `transport_mode == "mock"` 时，追问发送不调用 OpenClaw Gateway。
2. `transport_mode == "mock"` 时，追问消息写入 mock 微信消息文件。
3. `transport_mode == "mock"` 时，`/api/mock-wechat/conversations/{conversation_id}/messages` 能查到这条追问。
4. `transport_mode == "mock"` 时，案件详情里的聊天记录能看到这条追问。
5. `transport_mode == "gateway_rpc"` 时，原有真实 OpenClaw Gateway 发送逻辑保持不变。
6. 前端按钮不再显示“发微信”，而显示“发送”。
7. 前端成功提示不再显示“追问已通过微信桥发送”，而显示“追问已写入当前聊天通道”。
8. 失败提示不再显示“请检查微信桥配置”，而显示“请检查聊天通道配置”。
9. 后端测试通过。
10. 不泄漏任何 token、key、密钥。

---

## 13. 推荐最终代码形态

最终 `send_follow_up_question` 的核心发送部分应该类似下面这样。

注意：这是片段，不要照抄整个函数覆盖其它校验逻辑。

```python
content = (payload.content if payload and payload.content else question.content).strip()
if not content:
    raise HTTPException(status_code=422, detail="Follow-up content is empty")

if using_mock_wechat():
    new_msg = mock_wechat.append_message(
        conversation_id=conversation.id,
        sender="owner",
        content=content,
    )
    message = WechatMessage.model_validate(new_msg)
    mock_wechat.sync_to_json_store(store)
    event_type = "follow_up.sent_mock"
    event_title = "通过演示模式发送追问"
else:
    message = await OpenClawWechatAdapter(connection()).send_wechat_message(
        conversation_id=conversation.openclaw_conversation_id,
        content=content,
    )
    message.conversation_id = conversation.id
    store.add("wechat_messages", message)
    event_type = "follow_up.sent"
    event_title = "通过 OpenClaw 发送追问"

conversation.last_message_at = message.created_at
store.update("wechat_conversations", conversation)

question.content = content
question.status = "sent_via_openclaw" if message.status == "sent_via_openclaw" else "failed"
question.sent_message_id = message.id
question.failure_reason = (
    str(message.raw_payload.get("error")) if message.raw_payload and message.raw_payload.get("error") else None
)
question.updated_at = now_iso()
store.update("follow_up_questions", question)
record(
    event_type,
    event_title,
    content[:120],
    entity_type="case",
    entity_id=case_id,
)
return {"question": question, "message": message}
```

---

## 14. 常见错误提醒

不要犯这些错误：

1. 不要在 mock 分支用 `conversation.openclaw_conversation_id`。
2. 不要在 mock 分支调用 `OpenClawWechatAdapter`。
3. 不要只改 adapter，不改 `send_follow_up_question`。
4. 不要忘记 `mock_wechat.sync_to_json_store(store)`。
5. 不要忘记更新 `conversation.last_message_at`。
6. 不要忘记更新 `follow_up_questions` 表。
7. 不要新增消息状态。
8. 不要改大模型相关环境变量。
9. 不要把前端按钮逻辑删掉。
10. 不要把“发送”按钮改成不可用。
11. 不要删除已有测试。
12. 不要用真实微信做测试。

---

## 15. 完成后输出

完成修改后，请只汇报：

1. 修改了哪些文件。
2. mock 模式下追问发送现在如何工作。
3. gateway_rpc 模式是否保持原逻辑。
4. 跑了哪些测试，结果如何。

不要输出密钥。
不要输出 `.env` 内容。
不要输出无关文件内容。
